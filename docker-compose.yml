services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: qrious
      POSTGRES_PASSWORD: qrious
      POSTGRES_DB: qrious
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
